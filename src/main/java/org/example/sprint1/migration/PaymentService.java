package org.example.sprint1.migration;

public class PaymentService {
    public boolean processPayment(double amount) {
        // Базовая логика обработки платежа
        return amount > 0;
    }
}
